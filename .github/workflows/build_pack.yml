name: Build and Package

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  release:
    types: [created]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: List all files
      run: |
        Write-Host "📁 Repository files:"
        Get-ChildItem -Recurse -File | Select-Object Name, Length

    - name: Check for executable
      run: |
        # 检查exe文件
        $exeFiles = Get-ChildItem -Filter "*.exe" -File
        if ($exeFiles) {
          Write-Host "✅ Found executable files:"
          $exeFiles | ForEach-Object { Write-Host "  - $($_.Name) ($([math]::Round($_.Length/1MB, 2)) MB)" }
        } else {
          Write-Host "⚠️ No exe files found in repository"
          exit 1
        }

    - name: Create distribution package
      run: |
        # 1. 创建发布根目录
        New-Item -ItemType Directory -Force -Path "dist"

        # 2. 复制主程序
        $mainExe = Get-ChildItem -Filter "实时打字翻译.exe" -File
        $mainAhk = Get-ChildItem -Filter "实时打字翻译.ahk" -File

        if ($mainExe) { Copy-Item $mainExe -Destination "dist/" }
        if ($mainAhk) { Copy-Item $mainAhk -Destination "dist/" }

        # 3. 复制配置和说明
        Copy-Item "config.example.json" -Destination "dist/config.json"
        Copy-Item "README.md" -Destination "dist/"

        # 4. 创建 lib 目录并复制 .ahk 脚本
        New-Item -ItemType Directory -Force -Path "dist\lib"
        Copy-Item -Path "lib\*.ahk" -Destination "dist\lib\" -Force

        # 5. 复制 utility 文件夹（LOL 相关功能依赖）
        if (Test-Path "utility") {
            Copy-Item -Path "utility" -Destination "dist\" -Recurse -Force
            Write-Host "✅ Copied utility directory"
        }

        # 6. 复制 dll_64 文件夹
        if (Test-Path "lib\dll_64") {
            Copy-Item -Path "lib\dll_64" -Destination "dist\lib" -Recurse -Force
            Write-Host "✅ Copied dll_64 directory"
        }

        # 7. 列出最终打包内容以供检查
        Write-Host "📦 Final Package contents:"
        Get-ChildItem -Path "dist" -Recurse | Select-Object FullName, Length

    - name: Create ZIP archive
      run: |
        # 【修改点】文件名增加了 -LLM
        # 使用 7zip 进行压缩
        7z a -tzip "Real-time-translation-typing-LLM.zip" ".\dist\*" -mx9

        Write-Host "📦 Archive created:"
        Get-Item "Real-time-translation-typing-LLM.zip" | Select-Object Name, Length

    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: Real-time-translation-typing-LLM # 这里 Artifact 名字也顺便改对了
        path: dist/

    - name: Upload ZIP artifact
      uses: actions/upload-artifact@v4
      with:
        name: release-pkg
        # 【修改点】路径匹配新的文件名
        path: Real-time-translation-typing-LLM.zip

  release:
    needs: build
    runs-on: windows-latest
    if: github.event_name == 'release'
    permissions:
      contents: write

    steps:
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        name: release-pkg

    - name: Display Downloaded Files
      run: ls -R

    - name: Upload release asset
      uses: softprops/action-gh-release@v1
      with:
        files: |
          **/*.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}